class Playlist {
    // static all = []

    constructor({title, description, id, songs}) {
        this.title = title;
        this.description = description;
        this.id = id;
        this.songs = songs;
    }
}